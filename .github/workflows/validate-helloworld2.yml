name: Validate helloworld2 Proxy

on:
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run smoke test
        run: ./tests/helloworld2/smoke-test.sh
